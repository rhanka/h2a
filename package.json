{
  "name": "h2a",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "typecheck": "tsc -b --pretty false",
    "test": "npm run build && node --test packages/h2a/test/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
