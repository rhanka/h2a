apiVersion: v1
kind: Service
metadata:
  name: h2a-mcp
  labels:
    app.kubernetes.io/name: h2a-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: h2a-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
