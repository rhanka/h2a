{
  "name": "@sentropic/remote-llm-gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.12",
    "@sentropic/llm-gateway": "^0.13.0",
    "@sentropic/llm-mesh": "^0.15.0",
    "hono": "^4.12.32"
  },
  "devDependencies": {
    "@types/node": "^22.19.18",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "vitest": "^3.2.4"
  }
}
