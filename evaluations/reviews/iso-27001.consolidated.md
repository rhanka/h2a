# iso-27001.md — consolidated review (2/3)

> Reviews on 2026-05-28 — R1 agy, R2 codex **both verdict `revise`** and strongly concur. R3 (claude/opus) **timed out** (exit 124) across two attempts (240s, 480s) — deferred. Run sequentially from a neutral tmp dir (autonomous loop). Accepted changes applied to `evaluations/iso-27001.md`.

## Consensus findings → action

| # | Finding (reviewers) | Severity | Applied change |
|---|---|---|---|
| 1 | Scope appears to **sign / produce evidence** (R1, R2) | high | Diagram: relabel the h2a node "model: scope · roles · signed artifacts · journal"; the evidence arrow now reads "**EXECUTIF/CONTROL emit** signed journal + attestations" — the scope never signs. |
| 2 | **"mandated PRINCIPAL"** is wrong — a PRINCIPAL owns, is not mandated (R1, R2) | high | Row 3: risk owner = the scope's `PRINCIPAL` (owns the risk; **delegates execution via `MANDATE`**). |
| 3 | **SoA modelled as a `CONTRACT`** — it is a unilateral declaration (R1, R2) | med | SoA row + Contracts-vs-policies + diagram: SoA is a signed, versioned **`POLICY`**, not a bilateral contract. |
| 4 | **Certification body imposes** the policy (R2) | high | Cert-question #5 + hypothesis + diagram: the certification body is an external **`CONTROL`** (audits conformity); the imposing **`AUTHORITY`** is the standard/regulator/customer. |
| 5 | **Management review conflated with audit/CONTROL** (R1, R2) | med | Diagram: cl.9.2 internal audit → `CONTROL`; cl.9.3 mgmt review → `EXECUTIF`. |
| 6 | **Escalation target unspecified** (R1, R2) | med | Row 3 + ENFORCEMENT_PLAN: escalate to the scope's competent authority (`PRINCIPAL`/`EXECUTIF`). |
| 7 | **Clause 7 (Support / documented information) under-mapped** (R1, R2) | med | Added a `cl.7.5` row (journal = controlled records; competence/awareness external) + an `SP` diagram node. |
| 8 | **Coverage marks overclaim** h2a enforcement (R2) | med | Downgraded Operation (cl.8) and Nonconformity (cl.10) `✅`→`~` (h2a records/journals; it does not perform the control or validate effectiveness). |

## Rejected (verified false-positive)
- R1 + R2 flagged **`AMENDMENT`** (and `recourse`) as "invented" non-canonical terms. **Checked against source: `AMENDMENT` IS canonical** (`H2A_ARTIFACT_KINDS` in `packages/h2a/src/types.ts`; `H2AAmendment`/`isAmendment` in `artifacts.ts`), and `recourse` is a canonical mechanism (`packages/h2a/src/recourse.ts`). The reviewers lacked repo access (the doc was reviewed inline). **Not removed** — kept; the diagram/row wording now reads "corrective `ENGAGEMENT` + `AMENDMENT`". This is the demonstrated-facts discipline: verify before accepting a review finding.

## Mechanism note
agy + codex completed in the foreground with stdin from `/dev/null` (a prior background run hit 124-timeouts because codex/agy blocked on an absent stdin/tty). claude/opus timed out on this longer doc twice — R3 to be retried in a later loop pass.
