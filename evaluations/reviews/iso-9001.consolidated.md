# iso-9001.md — consolidated review (2/3)

> Reviews on 2026-05-28 — R1 [agy](./iso-9001.r1-agy.md), R2 [codex](./iso-9001.r2-codex.md) **both verdict `revise`**, strongly concurring (and mirroring the iso-27001 sibling). R3 (claude/opus) deferred — consistent timeouts on the cert docs. Run foreground from a neutral tmp dir (autonomous loop). Accepted changes applied to `evaluations/iso-9001.md`.

## Consensus findings → action

| # | Finding (reviewers) | Severity | Applied change |
|---|---|---|---|
| 1 | **"mandated PRINCIPAL"** — a PRINCIPAL owns, is not mandated (R1, R2) | high | Process-approach row: owner = the scope's `PRINCIPAL` (owns; delegates execution via `MANDATE` to `MANDATAIRE`/`AGENTS`). |
| 2 | **Management review mapped to `CONTROL`** (R1, R2) | high | Diagram: cl.9.2 internal audit → `CONTROL`; cl.9.3 mgmt review → `EXECUTIF`. |
| 3 | **Certification body conflated with the imposing AUTHORITY** (R1, R2) | high | Q5 + hypothesis + diagram: certification body = external **`CONTROL`** (audits); imposing party = external **`AUTHORITY`**. |
| 4 | **Scope implied as signer** (R1) | med | Diagram relabel: "h2a model: scope · roles · signed artifacts · journal"; evidence arrow "EXECUTIF/CONTROL emit…". |
| 5 | **`ENGAGEMENT` scope not explicit** (R1, R2) | med | "one **scoped** `ENGAGEMENT` per QMS process under the QMS scope"; ENGAGEMENT bullet names the owning `PRINCIPAL` + scope. |
| 6 | **Monitoring over-mapped to `CONTROL`** (R2) | med | Split cl.9.1 monitoring (process evidence under `ENGAGEMENT`/`ENFORCEMENT_PLAN`) from cl.9.2 internal audit (`CONTROL`). |
| 7 | **"ISO 9001 certifies a QMS" inaccurate** (R2) | med | Intro: ISO 9001 *specifies QMS requirements*; a certification body certifies conformity. |
| 8 | **Coverage overclaim** (R2) | med | Operation (cl.8) `✅`→`~`; monitoring split to `~`. |
| 9 | **Interested parties typed as `PRINCIPAL`s** (R2) | low | Row 1: interested parties are external actors; only owning/accountable parties are `PRINCIPAL`s. |
| 10 | **Escalation target** (R1) | low | ENFORCEMENT_PLAN: escalate to the scope's competent authority (`PRINCIPAL`/`EXECUTIF`). |

`AMENDMENT`/`recourse` were **not** flagged here (the rubric pre-stated they are canonical, after the iso-27001 false-positive). Same demonstrated-facts discipline.
