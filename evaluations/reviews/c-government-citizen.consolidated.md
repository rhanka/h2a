# c-government-citizen.md — consolidated review (2/3)

> 2026-05-28 — R1 agy (`revise`), R2 codex (`accept-with-changes`). R3 (claude) deferred (timeouts). Mature eval; findings verified vs source.

## Applied (valid)
- **Profile id + delta** (both): named `C_GOVERNMENT_CITIZEN` + deltas (external mandatory POLICY by scope, recourse AUTHORITY/tribunal, citizen-PRINCIPAL under imposed authority).
- **Mermaid label quoting** (agy): quoted the two risky edge labels (parentheses / comma) — `"public POLICY (imposed)"`, `"presents the question, does not judge"` — to avoid renderer fragility.

## Not applied (rejected / deferred)
- "10-question grid absent" (both, high) — **deferred, not a model requirement**: the README's common grid is a shared reference; the evals cover it via Diagram/Mapping/Patterns/Gaps/Hypothesis. Reproducing a full grid table in every mature eval is a structural preference, not a fidelity fix.
- ENGAGEMENT-scope-explicit / recourse-topology / casing — low-value nits on a mature eval; deferred.
- MANDATAIRE-as-presenter: the eval **already** states "presents the question, does not judge" — invariant correctly handled.
